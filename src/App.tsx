import "./App.css";
import { Dropdown } from "./components";

export const App = () => {
  return (
    <div>
      <section>
        <h1 className="section-title">Dropdown</h1>
        <Dropdown />
      </section>
    </div>
  );
};
